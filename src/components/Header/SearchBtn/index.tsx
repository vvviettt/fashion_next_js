import Image from "next/image";

const SearchBtn = () => {
  return (
    <div className="">
      <Image src={"/images/search.webp"} alt="" width={30} height={30} />
    </div>
  );
};

export default SearchBtn;
